# 実装計画: ローマ字→日本語変換 Web アプリ（フェーズ 000）

ゴール: `000-000_ローマ字日本語変換Webアプリ.md`

## 前提メモ

- API キーは `.env` の `LLM_INPUT_API_KEY`（OpenRouter、本プロジェクト専用、週 $3 上限）
- TOML の `api_key_env` で上記変数名を参照する
- 変換実行モデルは暫定値で実装し、完成後の実測で確定する
- 統合テストの実 API 呼び出しは最小回数・短い入力に抑える

## チェックリスト

- [x] 依存関係とパッケージ骨格を追加する（FastAPI / Uvicorn / OpenAI SDK / python-dotenv 等）
- [x] `config.toml` 読込と `.env.example` を追加する（`api_key_env`、bind 必須・デフォルトなし）
- [x] 変換ロジックの版管理を実装する（現行保存、更新時アーカイブ、CLI で復元）
- [ ] 確定／保存の永続化を実装する（入力・自動変換結果・確定文・ロジック版）
- [ ] OpenAI 互換 LLM クライアントを実装する（変換用・ロジック改訂用）
- [ ] FastAPI エンドポイントを実装する（変換・確定保存・ロジック更新・現行ロジック取得）
- [ ] 左右2ペイン Web UI を実装する（debounce 自動変換、確定、ロジック更新）
- [ ] アプリ起動入口を整え、README に起動手順を書く
- [ ] 統合テストを追加し、実 API で最小限の変換を確認する

## 構成案

```text
config.toml
.env / .env.example
src/romaji/
  app.py          # FastAPI
  config.py
  llm.py
  logic.py
  store.py
  cli.py
static/
  index.html / app.js / style.css
data/
  logic/current.md
  logic/versions/
  examples/
tests/
```

## 設定の想定キー（`config.toml`）

- `[server]` `host`, `port`（必須）
- `[llm]` `base_url`, `api_key_env`
- `[llm.conversion]` `model`（暫定）
- `[llm.logic_revision]` `model`
- パス類（ロジック／確定例のディレクトリ）

## 変換実行モデル（暫定）

実装時の初期値: `google/gemini-3.1-flash-lite-preview`  
確定は後続の実測で行う。
