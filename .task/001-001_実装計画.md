# 実装計画: ログイン認証機能とパスワード発行 CLI（フェーズ 001）

ゴール: `001-000_ログイン認証機能とパスワード発行CLI.md`

## チェックリスト

- [x] `romaji.auth` モジュールを実装し、パスワードハッシュ・検証、ランダムパスワード生成、ユーザーデータ保存（`data/auth`）を実装する
- [x] CLI に `issue-password` コマンドを追加し、admin / guest のパスワード自動発行および上書きを実装する
- [ ] `config.toml` に `auth_dir` を追加し、`.gitignore` で認証データを追跡対象外にする
- [ ] セッショントークン生成・検証（HMAC署名付きトークン）と FastAPI 認証・認可依存関係（Depends）を実装する
- [ ] FastAPI にログイン (`/api/auth/login`), ログアウト (`/api/auth/logout`), ユーザー情報取得 (`/api/auth/me`) エンドポイントを実装する
- [ ] 各 API エンドポイントにロール別認可制御（guest による確定保存・ロジック更新の拒否、未認証のアクセス拒否）を適用する
- [ ] Web UI にログイン画面、ユーザー状態表示、ログアウト機能、および guest 権限に応じた UI 制御（保存・更新ボタンの非表示／無効化）を実装する
- [ ] 認証・認可・CLI パスワード発行の単体テストおよび統合テストを追加し、すべてのテストを検証する
